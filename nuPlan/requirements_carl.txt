# Core simulation infrastructure (needed for carl_nuplan.planning.gym.environment.*)
nuplan-devkit>=1.1.1
scikit-learn
timm

# Utilities used by CaRL's env and model code
schedulefree
jsonpickle
tensorboardX
pytictoc

# Logging (optional)
wandb

# NOTE: the following CaRL training-loop packages are intentionally omitted because
# PRISM uses its own training infrastructure (scripts/train.py).
# gym==0.17.2          ← incompatible with Python 3.11 build tools
# pytorch-lightning    ← only used by CaRL's CARLA / PlanT training, not nuPlan gym
# torchmetrics         ← only used by pytorch-lightning
# gymnasium, torch, torchvision, torchaudio are expected to be pre-installed
